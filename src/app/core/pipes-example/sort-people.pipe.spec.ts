import { SortPeoplePipe } from './sort-people.pipe';

describe('SortPeoplePipe', () => {
  it('create an instance', () => {
    const pipe = new SortPeoplePipe();
    expect(pipe).toBeTruthy();
  });
});
